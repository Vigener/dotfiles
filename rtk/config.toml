# RTK (Rust Token Killer) — shared config
# Canonical: ~/dotfiles/rtk/config.toml
# Linux:  ~/.config/rtk/config.toml → symlink
# macOS:  ~/Library/Application Support/rtk/config.toml → symlink

[tracking]
enabled = true
history_days = 90

[display]
colors = true
emoji = false
max_width = 120

[filters]
ignore_dirs = [".git", "node_modules", "target", "__pycache__", ".venv", "vendor"]
ignore_files = ["*.lock", "*.min.js", "*.min.css"]

[tee]
enabled = true
mode = "failures"
max_files = 20
max_file_size = 1048576

[telemetry]
enabled = false

[hooks]
exclude_commands = ["git rebase", "git cherry-pick"]
transparent_prefixes = []

[limits]
grep_max_results = 200
grep_max_per_file = 25
status_max_files = 15
status_max_untracked = 10
passthrough_max_chars = 2000

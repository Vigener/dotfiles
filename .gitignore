# macOS
.DS_Store

# Nested dump (own .git; not a submodule)
autohotkey/win-env/

# Editor temp files
*.swp
*.swo
*~

# Node.js dependencies
node_modules/
npm-debug.log*

# Vibe Dictator vocabulary
vocabulary.json

# Typst build outputs
*.pdf

# Logs and session files
*.log
session.json

# herdr machine-local (shared track is config.toml only)
herdr/.config/herdr/plugins.json
herdr/.config/herdr/plugins/
herdr/.config/herdr/release-notes.json
herdr/.config/herdr/.plugins.lock
herdr/.config/herdr/*.sock

# rtk: only config.toml + README are tracked under rtk/
rtk/filters.toml
rtk/**/*.db
rtk/tee/

